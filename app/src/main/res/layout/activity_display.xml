<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:focusable="true" android:focusableInTouchMode="true">

    <EditText
        android:id="@+id/display_busca"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableRight="@drawable/search"
        android:hint="Buscar livro"
        android:maxLines="1"
        android:lines="1"
        android:inputType="textImeMultiLine"
        android:imeOptions="actionDone"/>

    <ListView
        android:id="@+id/lista_livros"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/display_busca"/>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/display_botao_add"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="16dp"
        android:layout_marginRight="16dp"
        android:src="@drawable/lapis"
        android:onClick="acao_display" />

</RelativeLayout>
